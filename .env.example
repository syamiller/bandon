# Upstash Redis (required on Vercel for shared live scores)
# Create at https://console.upstash.com or via Vercel → Storage → Redis / Upstash
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Optional: local API port (default 3001)
# PORT=3001
